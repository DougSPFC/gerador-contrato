# Copie este arquivo para ".streamlit/secrets.toml" (uso local) e defina sua senha.
# Em produção (Streamlit Community Cloud), configure isso em:
# App -> Settings -> Secrets, ao invés de usar este arquivo.

app_password = "troque-esta-senha"
